// Where the JSON API lives. Written at deploy time; the page falls back to
// same-origin if this file is missing, so the backend can also serve it.
window.NUPCO_API = "https://nupco-tender-api.onrender.com";
